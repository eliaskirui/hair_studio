{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: deploy",
            "request": "launch",
            "command": "npm run deploy",
            "cwd": "${workspaceFolder}"
        }
    ]
}